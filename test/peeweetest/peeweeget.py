#!/usr/bin/python
from sophdb import user as t



def insert():
    t.id = 1
    t.name = 'alvin'
    t.age =
def query():
    t_list = test_table.select().where(test_table.id << (1,2) )
    for T in t_list:
        #print(T.id,T.name)
        fileContent =

checkGold()